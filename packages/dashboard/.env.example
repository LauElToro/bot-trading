# Toro dashboard env
# Copy to .env.local and fill in.
#
# In dev, leave VITE_API_BASE_URL empty (or "/") so the Vite proxy
# in vite.config.ts handles routing to the backend.
# Override VITE_BACKEND_TARGET to point the proxy at a remote backend.
#
# On Vercel the SPA is a different origin than the bot. Set
# VITE_API_BASE_URL to the public bot URL (https://bot.example.com)
# and keep VITE_BASE_PATH=/ so the landing is / and the app is /dashboard.

VITE_API_BASE_URL=
VITE_BACKEND_TARGET=http://localhost:3848
VITE_BASE_PATH=/
VITE_GOOGLE_CLIENT_ID=
